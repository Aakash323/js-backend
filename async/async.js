// methods like setInterval(func , time) setTimout(func , time) are used to implemenmt async js
// other useful methods are clearInterval(reference)  and clearTimeout()

// setInterval runs until stop setTimeout runs only 1 time